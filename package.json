{
  "name": "cloudwalk-order-analytics",
  "version": "1.0.0",
  "private": true,
  "description": "An\u00e1lise de pedidos (Python) + dashboard de risco (Next.js/Tailwind)",
  "scripts": {
    "report": "python3 backend/analyzer.py --output frontend/public/report.json",
    "test:backend": "python3 -m pytest",
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix frontend run start",
    "lint": "npm --prefix frontend run lint",
    "setup": "npm --prefix frontend install && npm run report",
    "verify": "npm run test && npm run report && npm --prefix frontend run build:verify",
    "test:frontend": "npm --prefix frontend run test",
    "test": "npm run test:backend && npm run test:frontend"
  }
}
